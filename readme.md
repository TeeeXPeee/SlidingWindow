Java Sliding Window

- Helper library for creating and managing sliding windows for both character and numerical based problems
- Offers PreSum a window type to accelerate numerical based problems at a slight memory cost
- Offers BigDecimal window type to increase accuracy precision of numeric actions
- Use of BigDecimal comes with a high computation cost